## 🚀 Cómo ejecutar el proyecto:
1. Clonar el repositorio.
2. Crear el entorno virtual: `python -m venv venv`
3. Activar el entorno e instalar dependencias: `pip install flask pymongo`
4. Ejecutar la app: `python app.py`
